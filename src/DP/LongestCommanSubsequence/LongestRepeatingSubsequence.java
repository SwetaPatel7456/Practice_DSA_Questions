package DP.LongestCommanSubsequence;

public class LongestRepeatingSubsequence {

    //same as  lcs
    //have to add one extra thing
    // a[] = str1.toCharArray();
    //b[] = str1.toCharArray();
    // if(a[i-1]==b[j-1]  && i!=j)
}
